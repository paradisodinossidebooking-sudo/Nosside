// Paradiso di Nosside — script del sito
// Il sito e' interamente statico: la navigazione fra le sezioni usa gli
// anchor link e lo scorrimento fluido e' gestito in CSS (scroll-behavior).
// Questo file e' pronto per eventuali funzioni future (form, gallery, ecc.).

document.addEventListener('DOMContentLoaded', function () {
  // Aggiungi qui il codice JavaScript.
});
